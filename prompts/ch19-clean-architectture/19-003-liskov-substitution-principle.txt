Esnure that our PostgresSQLUserRepository implements IUserRepository interface exactly, maintaining the same error handling and return structures as our existing memory repository so they can be interchanged seamlessly.